print("Feedback Form")
print("Avantika university")
first_name=input("first name: ")
if(first_name.isdigit()):
    print("invalid input! the name can not contain digit")
    first_name=input("first name: ")
last_name=input("last name: ")
if(last_name.isdigit()):
    print("invalid input!the name can not contain digit")
    first_name=input("first name: ")
email=input("email:")
contact=input("contact number:")
if(len(str(contact))>10):
    print("invalid input")
    contact = input("enter the valid input:")

enroll_id=input("enrollment no:")
if(len(str(enroll_id))>9 || len(str(enroll_id))<9):
    print("invalid input:- your enrollment id is of only 9digits")
school==input("school:")
programme=input("programme:")
discipline=input("discipline:")
course=input("course:")
course_code=input("course code")
if(course_code!=DN2006Y23||course_code!=LA1006||course_code!=EN2005Y23||course_code!=EN2004Y23):
    print("invalid input!")
    course_code=input("enter valid course code:")

faculty=input("faculty name:")
sem=input("semester:")
check = input("Are you checked all the queries(reply in y/n)")
if(check==y):
    print("Thankyou for your valuable feedbac!",first_name)

#add feedback questions with input statement
